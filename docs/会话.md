请先不要直接改代码，先读取项目概要和当前验收上下文。

  必须先读：
  1. docs/project-onboarding-skill/SKILL.md
  2. docs/功能要求.md
  3. docs/性能要求.md
  4. docs/功能指标拆分与functions目录需求.md
  5. docs/功能要求实现完整性检查.md
  6. docs/functions实现完成度.md
  7. docs/功能测试视频展示文案.md
  8. docs/硬件配置.md

  然后读取前端/控制面/脚本入口：
  1. native_rdma/start.sh
  2. native_rdma/scripts/demo_up.sh
  3. native_rdma/scripts/start_node.sh
  4. native_rdma/control_plane/app.py
  5. native_rdma/control_plane/demo_orchestrator.py
  6. native_rdma/dashboard/index.html
  7. native_rdma/dashboard/*.js
  8. functions/common/run_one.sh
  9. functions/common/runner.py
  10. functions/common/checks.py
  11. functions/common/catalog.py
  12. functions/*/FN-*/run.sh
  13. performances/*/run.sh 或当前性能点实际入口

  项目事实：
  - 当前项目是 native-rdma-web / native_rdma 双节点 RDMA 项目。
  - 节点 A：xfusion3，RoCE IP 192.168.0.218。
  - 节点 B：xfusion4，RoCE IP 192.168.0.214。
  - 正常启动方式是在节点 A 执行：
    cd native_rdma
    bash start.sh
  - start.sh 会构建本地、同步到 xfusion4、构建远端、启动双节点。
  - 功能验收不允许伪造 PASS，不允许脚本直接制造结果。
  - 如果某项确实需要硬件或环境，必须输出 SKIP/WAIVED/FAIL 的真实原因。
  - 不能删除用户已有改动，不能回滚 unrelated changes。

  本次目标：
  解决“前端一键执行功能/性能点时偶发 FAIL”的问题。

  我希望你完整做以下事情：

  1. 先确认当前前端“运行当前功能点 / 运行当前性能点”的实际 API 调用链。
     - 找出点击按钮后调用的是哪个 Flask API。
     - 找出 API 最终执行的是哪个 shell/python 脚本。
     - 找出结果如何写入 summary.md、raw.json、history、前端状态。
     - 判断前端显示 PASS/FAIL 是读取实时执行结果，还是读取历史结果/聚合结果。

  2. 在节点 A 上执行：
     cd native_rdma
     bash start.sh
     这一步允许 ssh xfusion4、rsync、远端构建、远端启动。
     如果需要授权，请先说明用途并请求 approval。

  3. start.sh 成功后，逐个测试所有“功能点”的单点运行。
     - 模拟前端点击“运行当前功能点”，优先调用前端实际使用的 HTTP API，而不是只手动跑 shell。
     - 每个功能点都记录：模块、FN 编号、API、脚本入口、退出码、前端最终状态、summary/raw/log 路径。
     - 如果单独点击后从 PASS 变成 FAIL/SKIP，必须分析原因：
       - 是数据面未启动？
       - 是 peer_alive=false？
       - 是没有重新构建 C++ 数据面？
       - 是脚本默认环境变量不对？
       - 是 UDS 权限/路径不对？
       - 是运行顺序依赖？
       - 是前端读取旧 history 或覆盖 summary？
       - 是性能脚本与功能脚本共享状态互相污染？
       - 是某项本来需要 xfusion4/GPU/双节点，当前环境不满足？
     - 不允许只改前端显示成 PASS。

  4. 逐个测试所有“性能点”的单点运行。
     - 同样模拟前端点击“运行当前性能点”。
     - 每个性能点记录 API、脚本、日志、summary/raw、前端状态。
     - 如果失败，判断是脚本问题、数据面参数问题、性能阈值问题、数据面未重启问题、还是前端状态刷新问题。

  5. 如果发现问题，请直接修复。
     修复优先级：
     - 脚本环境变量缺失：在 run.sh 或 control_plane 调用处补齐。
     - 需要双节点 RDMA：让脚本明确检测 peer_alive，不要误报。
     - 需要同步 C++ 数据面：让文档和前端提示明确必须先 bash start.sh。
     - 前端/API 状态读取错误：修复控制面或前端刷新逻辑。
     - 单点运行覆盖已有 PASS：保留真实结果，但避免 sandbox/无 UDS 环境误覆盖有效手工结果；必要时将无法运行
标记为 SKIP，并在前端清楚显示原因。
     - 性能点需要独立启动参数：给性能 run 脚本加自包含检查或自动提示重启参数。

  6. 最终验收目标：
     - 我执行：
       cd native_rdma
       bash start.sh
     - 打开前端后，功能/性能总览显示应与真实 summary/raw 一致。
     - 我针对任意一个功能点点击“运行当前功能点”，执行完成并刷新后仍然是真实 PASS，除非该项确实是 WAIVED/
SKIP 且文档说明原因。
     - 我针对任意一个性能点点击“运行当前性能点”，执行完成并刷新后仍然是真实 PASS，或给出真实失败原因。
     - 不允许通过前端隐藏失败或伪造 summary/raw。

  7. 你需要最后输出：
     - 修复了哪些文件。
     - 每个功能点/性能点单点运行结果表。
     - 每个失败项的根因和修复方式。
     - 哪些项需要用户手动重新执行 bash start.sh。
     - 最终用户应执行的命令。
     - 如果仍有无法 PASS 的项，明确说明不可通过的真实原因。

  注意：
  - 当前 RDMA FN-4 GPU Direct RDMA 可能仍是 WAIVED，除非你实际完成 GPU Direct 数据面并在 xfusion4 验证。
  - 不要优先修改 storage/performance 之外的 unrelated 逻辑。
  - 不要为了前端好看伪造 PASS。


本会话允许在需要时请求授权执行 ssh xfusion4、rsync、远端构建、bash native_rdma/start.sh、以及调用本机前端HTTP API 做逐项验收。


功能点当前单点一键状态
  已经用前端实际 HTTP API 逐项跑过。修复后状态如下：

  | 模块 | 点位 | 当前状态 |
  |---|---|---|
  | storage | FN-1 | PASS |
  | storage | FN-2 | PASS |
  | storage | FN-3 | PASS |
  | storage | FN-4 | PASS |
  | storage | FN-5 | PASS |
  | storage | FN-6 | PASS |
  | rdma | FN-1 | SKIP，真实原因：该点需要 NR_TRANSPORT=tcp NR_ASYNC_REPL=0 对照模式，当前默认 RDMA 模式不
  适合直接判 FAIL |
  | rdma | FN-2 | PASS |
  | rdma | FN-3 | PASS |
  | rdma | FN-4 | WAIVED，真实原因：当前未启用/未完成 GPU Direct RDMA 数据面验证，不能伪造 PASS |
  | rdma | FN-5 | PASS |
  | mempool | FN-1 | PASS |
  | mempool | FN-2 | PASS |
  | mempool | FN-3 | PASS |
  | mempool | FN-4 | PASS |
  | mempool | FN-5 | PASS |
  | mempool | FN-6 | PASS，非破坏性 partial 验证 |

  结论：功能点层面，现在除 RDMA FN-1 的真实 SKIP、RDMA FN-4 的真实 WAIVED 外，其余都可以一键执行通过。

  性能点最后一轮 API 单点状态
  性能单点 API 测试结果保存在：

  - /tmp/native_rdma_performance_single_results_final.json

  最后一轮结果摘要：

  | 点位 | job 状态 | 前端/detail 状态 | 当前判断 |
  |---|---|---|---|
  | PF-1 | failed | FAIL | 还不能通过，需要继续查日志/raw |
  | PF-2 | finished | PASS | 可以一键通过 |
  | PF-3 | failed, rc=1 | PASS | 状态不一致，需要查脚本退出码和 raw/history 选择 |
  | PF-4 | finished | PASS | 可以一键通过 |
  | PF-5 | failed, rc=1 | PASS | 状态不一致，需要查脚本退出码和 raw/history 选择 |
  | PF-6 | failed, rc=1 | PASS | 状态不一致，需要查脚本退出码和 raw/history 选择 |
  | PF-7 | finished | PASS | 可以一键通过 |
  | PF-8 | finished | PASS | 可以一键通过 |
  | PF-9 | failed, rc=1 | PASS | 状态不一致，需要查脚本退出码和 raw/history 选择 |

  结论：性能点现在明确可以一键通过的是 PF-2、PF-4、PF-7、PF-8。PF-1 仍 FAIL。PF-3/PF-5/PF-6/PF-9 存在“API
  job 失败但前端/detail 显示 PASS”的不一致，不能视为完全验收通过，需要继续查对应 history/raw/log。

  下一步最该做

  1. 读取 /tmp/native_rdma_performance_single_results_final.json。
  2. 逐个打开 PF-1、PF-3、PF-5、PF-6、PF-9 最新 history 下的 raw.json、summary.md、日志。
  3. 判断 rc=1 是脚本真实失败、恢复数据面失败、阈值失败，还是 Flask detail 读取了旧 PASS。
  4. 修正后重新跑性能单点 API。
