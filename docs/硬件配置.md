# RDMA 硬件支持记录

采集时间：2026-04-27 17:25:34 CST  


## 结论摘要

| 项目 | 当前服务器 / 节点 A | xfusion4 / 节点 B |
|---|---|---|
| 主机名 | `xfusion3` | `xfusion4` |
| RDMA IP | `192.168.0.218/24` (`ens2np0`) | `192.168.0.214/24` (`ens6np0`) |
| RDMA 设备 | `mlx5_0` | `mlx5_0` |
| 网卡 | Mellanox MT27800 / ConnectX-5 (`MT4119`) | Mellanox MT27800 / ConnectX-5 (`MT4119`) |
| 链路 | Ethernet / RoCE, Active, LinkUp | Ethernet / RoCE, Active, LinkUp |
| 速率 | 100 Gb/sec | 100 Gb/sec |
| IPv4 RoCEv2 GID index | `3` | `3` |
| HCA NUMA node | `0` | `1` |
| 内核 | 5.15.0-139-generic | 5.15.0-171-generic |
| OS | Ubuntu 20.04.6 LTS | Ubuntu 22.04.5 LTS |
| GPU | 无可用 NVIDIA 驱动 / `nvidia-smi` 不可用 | 1 x A100 80GB PCIe + 1 x A800 80GB PCIe |

当前两台机器满足 RDMA 基础硬件条件：`mlx5_0` 均为 Active/LinkUp，RoCE IPv4 GID 可用，双向 IP ping 正常，RDMA WRITE 带宽/延迟测试可跑通。

## 1.1 操作系统与内核

| 项目 | xfusion3 | xfusion4 |
|---|---|---|
| `uname -a` | `Linux xfusion3 5.15.0-139-generic #149~20.04.1-Ubuntu SMP Wed Apr 16 08:29:56 UTC 2025 x86_64` | `Linux xfusion4 5.15.0-171-generic #181-Ubuntu SMP Fri Feb 6 22:44:50 UTC 2026 x86_64` |
| OS | Ubuntu 20.04.6 LTS (Focal) | Ubuntu 22.04.5 LTS (Jammy) |
| kernel >= 5.4 | 是 | 是 |
| glibc | `ldd (Ubuntu GLIBC 2.31-0ubuntu9.18) 2.31` | `ldd (Ubuntu GLIBC 2.35-0ubuntu3.13) 2.35` |
| `/proc/cmdline` | `BOOT_IMAGE=/boot/vmlinuz-5.15.0-139-generic root=UUID=c1460a69-ede7-43b5-a413-c4733329d651 ro` | `BOOT_IMAGE=/boot/vmlinuz-5.15.0-171-generic root=UUID=bb87b17d-7c9e-4162-af60-e261e6776f98 ro` |

## 1.2 RDMA 硬件与软件栈

### xfusion3

- PCI 设备：`2a:00.0 Ethernet controller: Mellanox Technologies MT27800 Family [ConnectX-5]`
- verbs 设备：`mlx5_0`, node GUID `506b4b0300284fac`
- 固件：`16.35.3502`
- driver：`mlx5_core`, version `24.01-0.3.3`, based on `Korg 6.3-rc3`
- MLNX OFED：`MLNX_OFED_LINUX-24.01-0.3.3.1`
- RDMA userspace 包：`rdma-core 2307mlnx47-1.2401033`, `libibverbs`, `librdmacm`, `ibverbs-utils`, `python3-pyverbs`
- 端口状态：`PORT_ACTIVE`, `LinkUp`, rate `100 Gb/sec`, link layer `Ethernet`
- MTU：`max_mtu 4096`, `active_mtu 1024`
- GID：
  - index `2`: IPv4 `192.168.0.218`, RoCE v1
  - index `3`: IPv4 `192.168.0.218`, RoCE v2
- UCX：`ucx_info unavailable`

### xfusion4

- PCI 设备：`99:00.0 Ethernet controller: Mellanox Technologies MT27800 Family [ConnectX-5]`
- verbs 设备：`mlx5_0`, node GUID `506b4b0300284fb4`
- 固件：`16.35.4506`
- driver：`mlx5_core`, version `24.10-3.2.5`, based on `Korg 6.8-rc4`
- MLNX OFED：`MLNX_OFED_LINUX-24.10-3.2.5.0`
- RDMA userspace 包：`rdma-core 2410mlnx54-1.2410068`, `libibverbs`, `librdmacm`, `infiniband-diags`, `ucx 1.18.0`
- 端口状态：`PORT_ACTIVE`, `LinkUp`, rate `100 Gb/sec`, link layer `Ethernet`
- MTU：`max_mtu 4096`, `active_mtu 1024`
- GID：
  - index `2`: IPv4 `192.168.0.214`, RoCE v1
  - index `3`: IPv4 `192.168.0.214`, RoCE v2
- UCX transport：检测到 `rc_verbs`, `ud_verbs`, `dc_mlx5`, `rc_mlx5`, `ud_mlx5`, `tcp`, `cma`

### 软件栈注意事项

- 两端 perftest 版本不一致：
  - xfusion3：`ib_write_bw/ib_write_lat Version: 5.60`
  - xfusion4：`ib_write_bw/ib_write_lat Version: 6.23`
- 直接互跑时，xfusion4 perftest 6.23 报错：`Current implementation is not compatible with versions older than 5.70`
- 本次压测为避免版本交换失败，将 xfusion3 的 perftest 5.60 临时复制到 `xfusion4:/tmp/ib_write_bw_5.60` 和 `xfusion4:/tmp/ib_write_lat_5.60` 后执行；未修改 xfusion4 系统安装版本。

## 1.3 RDMA 链路互通性压测

测试参数：

- 设备：`mlx5_0`
- GID index：`-x 3`（IPv4 RoCEv2）
- 链路类型：Ethernet/RoCE
- MTU：当前 active MTU `1024`
- `ib_write_bw` 默认消息大小：`65536` bytes
- `ib_write_lat` 默认消息大小：`2` bytes

### IP 连通性

| 方向 | 结果 |
|---|---|
| xfusion3 -> xfusion4 (`ping -c 3 192.168.0.214`) | 3/3 received, 0% loss, RTT min/avg/max/mdev = `0.086/0.119/0.168/0.035 ms` |
| xfusion4 -> xfusion3 (`ping -c 3 192.168.0.218`) | 3/3 received, 0% loss, RTT min/avg/max/mdev = `0.082/0.115/0.173/0.040 ms` |

### RDMA WRITE 带宽

| 方向 | 服务端 | 客户端 | BW peak | BW average | MsgRate |
|---|---|---|---:|---:|---:|
| A -> B | xfusion4 `/tmp/ib_write_bw_5.60` | xfusion3 `/usr/bin/ib_write_bw` | 57.40 Gb/sec | 57.39 Gb/sec | 0.109471 Mpps |
| B -> A | xfusion3 `/usr/bin/ib_write_bw` | xfusion4 `/tmp/ib_write_bw_5.60` | 42.98 Gb/sec | 41.86 Gb/sec | 0.079837 Mpps |

A -> B 客户端关键输出：

```text
#bytes     #iterations    BW peak[Gb/sec]    BW average[Gb/sec]   MsgRate[Mpps]
65536      5000             57.40              57.39              0.109471
```

B -> A 客户端关键输出：

```text
#bytes     #iterations    BW peak[Gb/sec]    BW average[Gb/sec]   MsgRate[Mpps]
65536      5000             42.98              41.86              0.079837
```

### RDMA WRITE 延迟

| 方向 | t_min | t_typical | t_avg | P99 | P99.9 / max |
|---|---:|---:|---:|---:|---:|
| A -> B | 1.71 us | 1.74 us | 1.74 us | 1.77 us | 4.39 us |
| B -> A | 2.02 us | 2.05 us | 2.06 us | 2.10 us | 6.15 us |

A -> B 客户端关键输出：

```text
#bytes #iterations    t_min[usec]    t_max[usec]  t_typical[usec]    t_avg[usec]    99% percentile[usec]   99.9% percentile[usec]
2       1000          1.71           4.39         1.74               1.74           1.77                   4.39
```

B -> A 客户端关键输出：

```text
#bytes #iterations    t_min[usec]    t_max[usec]  t_typical[usec]    t_avg[usec]    99% percentile[usec]   99.9% percentile[usec]
2       1000          2.02           6.15         2.05               2.06           2.10                   6.15
```

## 1.4 CPU / 内存 / NUMA / 存储

### CPU 与 NUMA

| 项目 | xfusion3 | xfusion4 |
|---|---|---|
| CPU | Intel Xeon Silver 4416+ | Intel Xeon Gold 5416S |
| 逻辑 CPU | 80 | 64 |
| Socket | 2 | 2 |
| Core / socket | 20 | 16 |
| Thread / core | 2 | 2 |
| NUMA node | 2 | 2 |
| NUMA CPU 分布 | node0 `0-19,40-59`; node1 `20-39,60-79` | node0 `0-15,32-47`; node1 `16-31,48-63` |
| HCA NUMA | node0 | node1 |
| `numactl -H` | 可用；node0 size `1031623 MB`, node1 size `1032127 MB` | `numactl: command not found` |

### 内存与 HugePages

| 项目 | xfusion3 | xfusion4 |
|---|---:|---:|
| `free -h` total | 2.0 TiB | 755 GiB |
| `free -h` available | 1.9 TiB | 694 GiB |
| `MemTotal` | 2113280932 kB | 792082688 kB |
| `AnonHugePages` | 7704576 kB | 0 kB |
| `HugePages_Total` | 0 | 0 |
| `HugePages_Free` | 0 | 0 |

当前两端均未预留 hugetlb HugePages。后续如果实现 hugepage + RDMA MR 内存池，需要单独配置。

### 存储设备

| 主机 | 设备 | ROTA | 容量 | 型号 / 类型 |
|---|---|---:|---:|---|
| xfusion3 | `sda` | 0 | 447.1G | `SE005-480GB-H` |
| xfusion3 | `sdb` | 0 | 21T | `MR9560-8i` |
| xfusion3 | `nvme1n1` | 0 | 1.9T | `ZHITAI TiPlus5000 2TB` / nvme |
| xfusion3 | `nvme0n1` | 0 | 1.8T | `RP2A21T9RK004LX` / nvme |
| xfusion4 | `sda` | 0 | 446.6G | `MR9560-8i` |
| xfusion4 | `sdb` | 0 | 10.5T | `MR9560-8i` |
| xfusion4 | `nvme0n1` | 0 | 1.8T | `Samsung SSD 990 PRO 2TB` / nvme |

`nvme list`：

- xfusion3：命令存在但未输出设备清单。
- xfusion4：`nvme command unavailable`。

### 文件系统与空间

| 主机 | 挂载点 | 设备 | 类型 | 容量 | 已用 | 可用 | 使用率 |
|---|---|---|---|---:|---:|---:|---:|
| xfusion3 | `/` | `/dev/sda2` | ext4 | 439G | 175G | 243G | 42% |
| xfusion3 | `/home` | `/dev/sdb1` | ext4 | 21T | 13T | 7.3T | 64% |
| xfusion3 | `/home/wsy/mnt` | `/dev/nvme1n1p2` | ext4 | 295G | 26G | 254G | 10% |
| xfusion3 | `/dev/shm` | tmpfs | tmpfs | 1008G | 16K | 1008G | 1% |
| xfusion4 | `/` | `/dev/sda2` | ext4 | 438G | 50G | 366G | 13% |
| xfusion4 | `/home` | `/dev/sdb1` | ext4 | 11T | 9.5T | 444G | 96% |
| xfusion4 | `/dev/shm` | tmpfs | tmpfs | 378G | 277M | 378G | 1% |

注意：xfusion4 的 `/home` 已使用 96%，后续大规模压测数据不宜继续落在 `/home`。

## 1.5 网络与防火墙

### xfusion3 网络

- `ens65f1`: `10.26.42.224/25`
- `ens2np0`: `192.168.0.218/24`，RDMA/RoCE 网段
- `zt2gceyjpu`: `10.107.232.190/24`
- 默认路由：`default via 10.26.42.129 dev ens65f1`
- RDMA 网段路由：`192.168.0.0/24 dev ens2np0 src 192.168.0.218`
- firewalld：inactive or not installed
- iptables：当前用户无法读取，报错 `Permission denied` / `/run/xtables.lock`

### xfusion4 网络

- `ens65f1`: `10.26.42.225/24`
- `ens6np0`: `192.168.0.214/24`，RDMA/RoCE 网段
- `docker0`: `192.168.200.1/24`
- 默认路由：`default via 10.26.42.129 dev ens65f1`
- RDMA 网段路由：`192.168.0.0/24 dev ens6np0 src 192.168.0.214`
- firewalld：inactive
- iptables：当前用户无法读取，`sudo` 需要密码

SSH 免密：已确认当前服务器可通过 `ssh xfusion4` 执行命令。

## 1.6 GPU 情况

### xfusion3

`nvidia-smi` 输出：

```text
NVIDIA-SMI has failed because it couldn't communicate with the NVIDIA driver.
no GPU
```

结论：当前服务器没有可用 NVIDIA GPU/驱动环境。

### xfusion4

`nvidia-smi`：

- Driver Version：`550.54.15`
- CUDA Version：`12.4`
- GPU0：`NVIDIA A100 80GB PCIe`, bus `00000000:AB:00.0`
- GPU1：`NVIDIA A800 80GB PCIe`, bus `00000000:E1:00.0`
- 当前有 `python` 进程占用两张 GPU，各约 23.4 GiB 显存。

`nvidia-smi topo -m`：

```text
        GPU0    GPU1    NIC0    CPU Affinity    NUMA Affinity
GPU0     X      SYS     SYS     16-31,48-63     1
GPU1    SYS      X      SYS     16-31,48-63     1
NIC0    SYS     SYS      X

NIC0: mlx5_0
```

结论：xfusion4 的 GPU 与 HCA 都在 NUMA node 1，但拓扑距离显示为 `SYS`，不是 `PIX/PXB/PHB` 级近邻。若后续实现 GPU-Direct RDMA，需要单独验证 GDRCopy/peer memory 和实际吞吐。

## 1.7 当前已有代码基线

仓库现有：

- `backend/`：旧版后端。
- `backend_v2/`：README 标记为“高性能 Ceph + RDMA backend”，项目 README 也将其标记为高性能重写主线。
- `backend_dev/`：当前开发工作目录，`backend_dev_prompt.md` 要求本轮开发先在此目录修改、同步到 xfusion4，最终再同步回 `backend/`。
- `dashboard/`：现有 HTML/JS 前端。

建议记录：

- 主线选择：以 `backend_v2/` 的结构和接口为基线，同时吸收 `backend_dev/` 中已完成的新功能；若进入“抛开 Ceph、自研 RDMA 数据面”的阶段，建议新建 C++ 数据面模块，不直接把 Python 热路径继续加重。
- 技术路线：采用 `C++17 + libibverbs/rdma-core` 写性能关键数据面；Python Flask 只保留控制面/API/演示面。
- 前端：保留现有 `dashboard/*.html` / JS 样式，新增 RDMA 实时吞吐、延迟分布、利用率和分级迁移面板即可。

## 1.8 交付目标与演示时间点

仓库和 `rdam.md` 中没有填写验收评审日期。

- 验收评审日期：待确认。
- 演示环境：当前硬件条件支持“仅两台真实节点”演示；如需要压测客户端或旁路观测，可再接第 3/4 台。
- 预录屏备选：待确认；建议准备，因为 `rdam.md` 第八/九部分也将预录视频列为现场网络波动兜底。

## 后续参数建议

- RDMA 设备固定使用 `mlx5_0`。
- RoCE 地址使用 `192.168.0.218 <-> 192.168.0.214`。
- verbs/perftest/后续程序应显式使用 GID index `3`。
- RDMA poller/数据面线程：
  - xfusion3 优先绑定 NUMA node 0。
  - xfusion4 优先绑定 NUMA node 1。
- 当前 active MTU 为 `1024`，低于设备 max `4096`；若要冲击更高带宽，建议后续统一网卡 MTU/active_mtu 后复测。
- HugePages 当前未配置；如采用 hugepage slab + RDMA MR，应先预留 hugepages。
- 两端 OFED/perftest 版本不一致，建议统一 perftest 或统一 MLNX_OFED，以免自动化压测和后端调用继续遇到版本交换失败。
