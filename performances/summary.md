# Performances Summary

当前汇总已失效：性能脚本已改为必须走数据面或指定工具，旧的本地替代测量结果不再有效。

请重新运行：

```bash
cd /home/wangshouxin/native-rdma-web/native_rdma
bash start.sh

cd /home/wangshouxin/native-rdma-web
bash performances/run_all.sh
```
