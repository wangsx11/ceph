# Codex 任务 Prompt — Ceph 测试自动修复

> 将以下内容完整复制给 Codex，作为任务起始 prompt。

---

你正在维护一个基于 Ceph + RDMA 的分布式存储系统，运行在三节点集群上（xfusion3 为主节点，xfusion4 和 xfusion5 提供额外磁盘）。代码库位于 xfusion3 的 `~/ceph-web` 目录下。

**你的任务：** 修复测试套件中所有失败的测试。`tests/` 下每个子目录都有一个 `result.md`，记录了当前的测试结果。很多测试因为运行时报错或代码 bug 而处于 FAILED 状态。

---

## 集群权限说明

- 该 Ceph 集群为个人独占使用，无其他用户，你可以对集群进行任何必要的操作。
- 对于 Ceph 相关的常规操作（pool 管理、OSD 操作、CRUSH 规则、pg 操作等），直接执行，无需顾虑影响他人。
- **但凡需要使用 `sudo` 的操作，必须在执行前向我明确声明**，说明要执行的具体命令和原因，等待我确认后再执行。不得静默使用 `sudo`。

---

## 操作规范

1. 先读取每个测试子目录的 `result.md`，明确失败原因。
2. 只修复失败的测试，**不要动任何已经通过的测试对应的代码**。
3. Ceph 集群在当前服务器上实时运行，除非测试明确要求，否则不要修改集群配置、pool 定义或 CRUSH 规则。
4. 如果修复需要了解集群实际状态，先执行 `ceph status`、`ceph osd tree` 或 `ceph df` 确认，不要凭假设修改。
5. 不要重构已通过的代码，只做最小范围的精准修复。

---

## 修复与测试流程

**每个失败测试严格按此循环执行：**

```
① 读取 result.md，定位失败原因
② 修改代码
③ 立即运行对应测试，捕获完整输出
④ 判断结果：
   - 通过 → 执行【成功处理】
   - 失败 → 执行【失败处理】，返回 ② 继续尝试
```

### 成功处理

- 将测试的实际输出结果更新到该子目录的 `result.md` 中，替换掉原来 FAILED 的部分，标注为 PASS 并附上本次完整输出。
- 继续处理下一个失败测试。

### 失败处理

在该测试子目录下维护一个 `debug_log.md` 文件（不存在则创建），每次失败尝试追加以下内容：

```markdown
## 尝试 N — YYYY-MM-DD HH:MM

### 修改内容
<描述本次改了什么>

### 测试输出
<完整的报错或输出>

### 失败原因分析
<分析为何仍然失败>
```

若同一个测试**连续失败 3 次**，停止尝试，在 `debug_log.md` 末尾追加：

```markdown
## 结论：暂无法自动修复，需人工介入
```

然后跳过该测试，继续处理其他失败项。

---

## 收尾同步规范

所有测试处理完毕后（无论通过还是跳过），执行一次性全量同步，将 xfusion3 上的完整 `~/ceph-web` 目录同步到 xfusion4，保持两台机器强一致：

```bash
rsync -avz --delete ~/ceph-web/ wangshouxin@xfusion4:~/ceph-web/
```

同步完成后输出确认信息，整个任务结束。

---

## 开始执行

现在开始，从 `tests/` 目录出发，逐一检查各子目录的 `result.md`，按上述流程依次处理所有 FAILED 项。