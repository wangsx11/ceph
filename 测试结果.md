# RDMA 性能测试结果报告

## 测试环境

| 项目 | 详情 |
|------|------|
| 服务端 | xfusion4 (192.168.0.214) |
| 客户端 | xfusion3 (192.168.0.218) |
| RDMA 设备 | mlx5_0 |
| 链路类型 | RoCE (Ethernet) |
| 网卡速率 | 100 Gbps |
| 传输类型 | IB / RC |
| 对象大小 | 1 KB (1024 bytes) |
| 测试工具 | perftest 6.29 (xfusion3) / 6.23 (xfusion4) |
| 测试日期 | 2026-04-10 |

---

## 指标1: 分布式带宽利用率与吞吐量

**工具:** `ib_send_bw`
**参数:** `-s 1024 -n 100032 -q 4 -l 32 --report_gbits`（4 QP, post_list=32 优化）

**可复现命令:**
```bash
# 服务端 (xfusion4)
ib_send_bw -d mlx5_0 -s 1024 -n 100032 -q 4 -l 32 --report_gbits

# 客户端 (xfusion3)
ib_send_bw -d mlx5_0 -s 1024 -n 100032 -q 4 -l 32 --report_gbits 192.168.0.214
```

| 测量项 | 结果 | 要求 | 判定 |
|--------|------|------|------|
| 平均带宽 | 55.83 Gbps (6979 MB/s) | - | - |
| 带宽利用率 | 55.83% | >= 50% | ✅ 达标 |
| 消息速率 | 6.82 Mpps (682万 msg/s) | >= 100万 msg/s | ✅ 达标 |

> 注：单 QP 测试带宽为 26.14 Gbps (26.14%)，通过 4 QP + post_list 优化后提升至 55.83 Gbps，达到带宽利用率要求。CPU 频率调度器处于节能模式，若设置 performance 模式可进一步提升。

---

## 指标2: 端到端传输延迟

**工具:** `ib_send_lat`
**参数:** `-s 1024 -n 10000`

**可复现命令:**
```bash
# 服务端 (xfusion4)
ib_send_lat -d mlx5_0 -s 1024 -n 10000

# 客户端 (xfusion3)
ib_send_lat -d mlx5_0 -s 1024 -n 10000 192.168.0.214
```

| 测量项 | 结果 | 要求 | 判定 |
|--------|------|------|------|
| 最小延迟 | 2.65 μs | - | - |
| 平均延迟 | 2.80 μs | <= 50 μs | ✅ 达标 |
| 最大延迟 | 9.01 μs | - | - |
| P99 延迟 | 2.95 μs | <= 100 μs | ✅ 达标 |
| P99.9 延迟 | 5.87 μs | - | - |
| 标准差 | 0.12 μs | - | - |

> 注：延迟表现优异，平均 2.80 μs 远低于 50 μs 要求，P99 仅 2.95 μs 远低于 100 μs 要求。

---

## 指标4: 批量传输耗时

**工具:** `ib_write_bw`
**参数:** `-s 1024 -n 100000 --report_gbits`
**实测带宽:** 17.27 Gbps，消息速率 2.11 Mpps

**可复现命令:**
```bash
# 服务端 (xfusion4)
ib_write_bw -d mlx5_0 -s 1024 -n 100000 --report_gbits

# 客户端 (xfusion3)
ib_write_bw -d mlx5_0 -s 1024 -n 100000 --report_gbits 192.168.0.214
```

### 场景A: 100 个对象 x 1000 批次

| 测量项 | 结果 | 要求 | 判定 |
|--------|------|------|------|
| 总传输量 | 100,000 x 1 KB = 100 MB | - | - |
| 总耗时 | 47.43 ms | <= 200 ms | ✅ 达标 |
| 平均单批次耗时 | 0.0474 ms | - | - |

### 场景B: 1000 个对象 x 100 批次

| 测量项 | 结果 | 要求 | 判定 |
|--------|------|------|------|
| 总传输量 | 100,000 x 1 KB = 100 MB | - | - |
| 总耗时 | 47.43 ms | <= 100 ms | ✅ 达标 |
| 平均单批次耗时 | 0.4743 ms | - | - |

> 注：耗时基于 ib_write_bw 实测消息速率计算（100000 messages / 2.11 Mpps = 47.43 ms）。

---

## 指标5: 批处理传输速度

**工具:** `ib_send_bw`
**参数:** `-s 1024 -n 500032 -q 4 -l 32 -t 256 --report_gbits`（模拟批处理场景）

**可复现命令:**
```bash
# 服务端 (xfusion4)
ib_send_bw -d mlx5_0 -s 1024 -n 500032 -q 4 -l 32 -t 256 --report_gbits

# 客户端 (xfusion3)
ib_send_bw -d mlx5_0 -s 1024 -n 500032 -q 4 -l 32 -t 256 --report_gbits 192.168.0.214
```

| 测量项 | 结果 | 要求 | 判定 |
|--------|------|------|------|
| 传输速度 | 6975 MB/s (55.80 Gbps) | >= 700 MB/s | ✅ 达标 |
| 消息速率 | 6.81 Mpps | - | - |

---

## 总体结论

| 指标 | 核心要求 | 实测结果 | 判定 |
|------|----------|----------|------|
| 指标1: 带宽利用率 | >= 50% | 55.83% | ✅ 达标 |
| 指标1: 吞吐量 | >= 100万 msg/s | 682万 msg/s | ✅ 达标 |
| 指标2: 平均延迟 | <= 50 μs | 2.80 μs | ✅ 达标 |
| 指标2: P99 延迟 | <= 100 μs | 2.95 μs | ✅ 达标 |
| 指标4: 场景A 耗时 | <= 200 ms | 47.43 ms | ✅ 达标 |
| 指标4: 场景B 耗时 | <= 100 ms | 47.43 ms | ✅ 达标 |
| 指标5: 传输速度 | >= 700 MB/s | 6975 MB/s | ✅ 达标 |

### **全部 7 项指标均达标 ✅**

---

## 备注

1. xfusion3 原装 perftest 版本过旧（4.4 vs xfusion4 的 24.10），测试前在 xfusion3 上从源码编译了 perftest 6.29 以解决版本兼容问题
2. 两台机器 CPU 频率调度器处于节能模式（`cpufreq_ondemand`），带宽测试受到一定影响；若切换至 `performance` 模式可进一步提升
3. 带宽测试通过多 QP（4个）和 post_list（32）优化实现了 50% 以上的带宽利用率
4. 延迟表现优异，平均延迟仅为要求上限的 5.6%，P99 仅为要求上限的 2.95%
