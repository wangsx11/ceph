## 演示内容

本项目评审现场拟进行原型系统演示，必须使用真实开发的原型系统提供演示，以视频结合PPT的形式呈现（提供演示所用原型系统的源代码、可独立运行的部署包备查，以符合真实性）。

### 1. 分布式共享数据管理中的对象总览与单对象详细状态可视化演示

a. 演示分布式共享数据管理中的对象全局总览界面，能够实时展示对象数量、类型、状态及占用资源等信息。

b. 演示点击任意对象，提供可观察的完整内部状态，包括字段结构、更新时间戳。

c. 演示过程展示出界面动态刷新、对象状态实时更新等可观测指标。

### 2. 对象生命周期管理与外界接口写操作演示

a. 演示通过系统提供的外部接口，对分布式共享数据管理中的对象执行创建、查询、修改、删除等操作。

b. 操作结果在分布式共享数据管理系统中实时生效，并在可视化界面同步呈现对象状态的变化。

### 3. 基于 RDMA 跨节点对象读写与数据同步能力演示

a. 构建不少于两个节点的运行环境，共享同一分布式共享数据空间。

b. 演示内容包括：在节点 A 写入对象，并在节点 B 进行读取、修改及回写操作，所有变更在两节点的可视化界面实时呈现。

c. 系统须展示数据同步延迟、跨节点传输行为等可观测指标。

d. **本地模拟跨节点的演示视为无效演示。**

### 4. 快照创建与恢复能力及耗时演示

a. 基于实际对象规模生成快照文件，并展示快照创建耗时、写入速率。

b. 展示快照文件大小等关键指标。

c. 演示执行快照恢复操作，并展示恢复耗时、恢复进度及恢复后对象一致性校验结果。

d. **快照未落盘的演示视为无效演示。**

### 5. 系统吞吐量及实体数量增加对性能影响演示

a. 演示在至少两个节点下逐步增加对象数量（例如 1 万、5 万、10 万等），并对系统施加持续并发读写负载。

b. 系统须实时展示 RDMA 实时吞吐量、延迟分布、网络使用率等指标，并以曲线形式展示实体数量增长对系统性能的影响。

c. **本地模拟跨节点的演示视为无效演示。**

### 6. 分级存储能力演示

a. 演示真实访问行为完成冷热数据识别，并在三层分级存储环境（内存、SSD、HDD）下自动执行数据提升与下沉。

b. 展示冷热数据迁移过程连续、可观测，展示迁移事件内容，包含时间戳、触发条件及迁移前后层级信息。

c. 演示冷数据下沉至容量层后自动触发备份或快照生成，并完整展示备份对象数量、耗时及生成结果；在后续访问中，系统能够自动识别热度变化并完成数据回迁。

d. **依赖静态数据或人工干预完成冷热数据识别、冷热迁移、备份行为的演示视为无效。**

## 多级异构的高效能存储模块

该模块是一个具备多级异构结构的高效能存储模块，应支持多级的存储层，包括构建内存层、高速存储层，能够存储大规模数据并提升访问速度，支持数据迁移和预取，根据访问频率将数据在低速和高速存储层之间进行迁移，根据数据的访问模式（顺序或随机）提前加载可能访问的数据块。须涵盖如下功能：

1. 提供仿真引擎异构存储功能，兼容多种存储设备，包括：NVMe / SATA 固态盘 / ZNS SSD 等新型存储设备，提供统一访问接口。
2. 提供多层感知与调度机制，该机制能够识别数据所属存储层级，并根据访问频率、局部性对存储数据进行冷热分离，以提升访问效率和吞吐量。
3. 提供文件访问的多策略预取机制，提升系统在实时仿真、仿真回放、结果分析等多种应用场景下的存储效率。
4. 提供可配置的数据压缩与去重机制，降低存储空间和网络传输开销，提升仿真数据存储效率与资源利用率。
5. 提供 IO 调度与优先级管理功能，将仿真过程中的 I/O 操作分为实时性要求高的前台 I/O（如仿真数据落盘、检查点、实时分析）和延迟容忍型的后台 I/O（如压缩、去重、垃圾回收），保障高优先级 I/O 操作。
6. 支持仿真数据运行中采集，支撑模块能够在仿真运行过程中及时捕获多类型的数据流，包括对象属性、交互事件等。

---

## RDMA 分布式仿真计算模块

RDMA 分布式仿真计算模块围绕软硬件协同的 RDMA 网络体系构建低延迟、高带宽的通信能力，向仿真应用提供标准化的通信接口，兼容 RDMA 协议和非 RDMA 环境下的 TCP/IP 协议以保障部署灵活性，缩减数据传输链路中的冗余路径，提供高效网络传输与远程存储加速能力。须涵盖如下功能：

1. 提供支持 RDMA 与传统 TCP/IP 的统一通信层，通过独立协议适配器实现协议之间的无冲突切换与运行。
2. 支持聚合数据传输功能，对小规模数据包或多个并发通信流进行高效聚合。
3. 支持流量优先级机制，根据不同数据流的优先级对网络资源进行调度和分配，保证关键数据优先传输。
4. 支持 CPU 与 GPU 之间的高速直通访问功能，能够使 GPU 直接读写远程内存，避免 CPU 参与数据搬运，降低通信延迟。
5. 支持分布式节点之间数据传输的路由转发、负载均衡。

---

## 一致性总线内存池化仿真计算模块

该模块提供一致性互联访问、跨节点高性能数据交换等机制，实现多节点共享内存同步访问，保障数据正确一致；利用 RDMA 的分布式内存池相关技术，屏蔽底层网络细节，提升开发效率与可移植性，运用并发访问控制、数据同步等机制，保护共享资源并发访问，保障数据有序同步。须涵盖如下功能：

1. 支持 RDMA 语义特性直接远程访问内存，消除数据在内核和用户空间之间不必要的 CPU 拷贝，提升数据传输的效率。
2. 提供分布式内存池基本功能，提供封装好的 API 接口，支持用户空间直接访问远程内存区域，屏蔽底层网络细节，提升开发效率与系统可移植性。
3. 提供内存池统一命名机制，用于标识不同仿真节点间共享的内存区域。
4. 支持跨节点内存自适应分配与热数据迁移，能够支持 RDMA 本地内存与远端内存的自适应分配，动态识别热点数据并将仿真数据存放于本地内存。
5. 提供任务级与用户级内存隔离机制，对不同仿真任务使用独立的内存空间，支持多用户之间的内存访问隔离。
6. 内存池提供高可靠机制，保证在部分节点故障的情况下，系统继续可用。

---

## 网存算一体化硬件基础支撑子系统功能

1. **RDMA 网络环境分布式通讯能力**：分布式的带宽利用率能达到 50% 以上，单个对象 1KB 时，吞吐量达到 100 万/s。
2. **RDMA 网络环境下对象传输能力**：端到端传输 10 万个仿真对象，单个对象 1KB 时，数据平均时延 ≤ 50 微秒，P99 响应时间 ≤ 100 微秒。
3. **RDMA 网络环境下 QoS（事件优先级传输能力）指标**：高优先级事件 2500 个，低优先级事件 2500 个，高优先级比低优先级处理效率提升 22%。
4. **RDMA 网络环境下对象数据聚合传输能力**：仿真对象数量为 100（单个对象 1KB）时，1000 批次传输总耗时 ≤ 200ms；仿真对象数量为 1000（单个对象 1KB）时，100 批次传输总耗时 ≤ 100ms。
5. **RDMA 网络环境下批处理能力**：仿真引擎单个对象 1KB 时，批处理（1000 批次及 100 批次）传输速度 ≥ 700MB/s。
6. **多级存储读写能力**：实现数据写入速率达到 10GB/s（全闪存阵列），数据读取速率达到 20GB/s（全闪存阵列）。


8. **RDMA 网络环境下仿真引擎运行能力**：节点数为 4 个，10 万仿真实体（大小 1KB），事件数量 100 万，仿真运行速度 ≥ 1 倍实时仿真推进速度。
9. **仿真引擎内存池化能力**：使用内存池化技术后，在性能损失不超过 5% 的前提下，内存利用节省 7%；在单节点多线程高并发环境下，对 1KB 对象内存分配/释放，对比使用前，吞吐量提升 ≥ 20%。